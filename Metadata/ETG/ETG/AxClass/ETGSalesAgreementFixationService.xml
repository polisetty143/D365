<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ETGSalesAgreementFixationService</Name>
	<SourceCode>
		<Declaration><![CDATA[
using Microsoft.Dynamics365.LocalizationFramework;

public class ETGSalesAgreementFixationService
{
    public const str ERFormatModelName          = 'Model';
    public const str ParametersDataSourceName   = 'RunTimeParameters';
    public const str reportFormatName           = 'ETG Sales Contract Fixation Format (Word)';

}
]]></Declaration>
		<Methods>
			<Method>
				<Name>generateReportByGER</Name>
				<Source><![CDATA[
    public void generateReportByGER(ETGSalesAgreementFixationContract _contract)
    {
        ERFormatMappingId               formatMappingId;
        ETGSalesAgreementFixationDP     dataProvider;
        ETG_SalesAgreementFixationTmp   salesAgreementFixationTmp;
        ERIFormatMappingRun             formatMappingRun;
        ERFormatMappingTable            formatMappingTable;

        select firstonly Name, RecId from formatMappingTable
            where formatMappingTable.Name == reportFormatName;

        dataProvider                =   ETGSalesAgreementFixationDP::construct();
        formatMappingId             =   formatMappingTable.RecId;
        salesAgreementFixationTmp   =   dataProvider.processReport(_contract);
        
        if (formatMappingId)
        {
            try
            {

                ERIModelDefinitionParamsAction  parameters = new ERModelDefinitionParamsUIActionComposite().
                    Add(new ERModelDefinitionDatabaseContext().addTemporaryTable(salesAgreementFixationTmp)).
                    Add(new ERModelDefinitionObjectParameterAction(ERFormatModelName, ParametersDataSourceName, _contract, true)).
                    Add(new ERModelDefinitionDatabaseContext().addValue(tableNum(CompanyInfo), fieldNum(CompanyInfo, DataArea),curExt()));
                
                formatMappingRun    =   ERObjectsFactory::createFormatMappingRunByFormatMappingId(formatMappingId);

                formatMappingRun.withParameter(parameters);
                formatMappingRun.parmFileName(reportFormatName);
                formatMappingRun.withFileDestination(_contract.getFileDestination());
                formatMappingRun.parmShowPromptDialog(false);
                formatMappingRun.run();

            }
            catch
            {
                info("@SYS300117");
            }
        }
        else
        {
            info("@SYS300117");
        }
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>