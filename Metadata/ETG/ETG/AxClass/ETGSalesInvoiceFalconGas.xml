<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ETGSalesInvoiceFalconGas</Name>
	<SourceCode>
		<Declaration><![CDATA[
internal final class ETGSalesInvoiceFalconGas
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>main</Name>
				<Source><![CDATA[
    /// <summary>
    /// Class entry point. The system will call this method when a designated menu
    /// is selected or when execution starts and this class is set as the startup class.
    /// This method will execute report run controller for ETGSalesPackingSlip ETGReportFR
    /// </summary>
    /// <param name = "_args">The specified arguments.</param>
    public static void main(Args _args)
    {
        
        CustInvoiceJour              custInvoiceJour;
        SrsReportRunController       reportRunController;
        SrsReportRunImpl             srsReportRun;
        SalesInvoiceContract         dataContract;

        if(_args.record() && _args.dataset() == tableNum(CustInvoiceJour))
        {
            custInvoiceJour = _args.record();
        }
        else
        {
            throw Error(Error::missingFormActiveBuffer(formStr(CustInvoiceJournal)));
        }

        reportRunController = new SrsReportRunController();
        dataContract = new   SalesInvoiceContract();
        reportRunController.parmReportName(ssrsReportStr(ETGSalesInvoice, ETGReport_N004));
        reportRunController.parmShowDialog(false);
        dataContract = reportRunController.parmReportContract().parmRdpContract() as SalesInvoiceContract;
        dataContract.parmRecordId(custInvoiceJour.RecId);
        // dataContract.parm(custInvoiceJour.TableId);
        srsReportRun = reportRunController.parmReportRun() as SrsReportRunImpl;
        reportRunController.parmShowDialog(false);
        reportRunController.parmReportRun(srsReportRun);
        reportRunController.parmDialogCaption('Sales invoice report - Falcon Gas Zambia');
        reportRunController.parmReportContract().parmPrintSettings().printMediumType(SRSPrintMediumType::Screen);
        reportRunController.startOperation();
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>