<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ETGPurchPackingSlipGRNReportFrench</Name>
	<SourceCode>
		<Declaration><![CDATA[
internal final class ETGPurchPackingSlipGRNReportFrench
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>main</Name>
				<Source><![CDATA[
    /// <summary>
    /// Class entry point. The system will call this method when a designated menu 
    /// is selected or when execution starts and this class is set as the startup class.
    /// </summary>
    /// <param name = "_args">The specified arguments.</param>
    public static void main(Args _args)
    {
        VendPackingSlipJour     VendPackingSlipJour;
        SrsReportRunController reportRunController;
        SrsReportRunImpl        srsReportRun;
        PurchPackingSlipContract dataContract;
        VendPackingSlipVersion  vendPackingSlipVersion;
 
        if(_args.record() && _args.dataset() == tableNum(VendPackingSlipJour))
        {
            VendPackingSlipJour = _args.record();
        }
        else
        {
            throw Error(Error::missingFormActiveBuffer(formStr(VendPackingSlipJournal)));
        }

        reportRunController = new SrsReportRunController();
        dataContract = new  PurchPackingSlipContract();
        reportRunController.parmReportName(ssrsReportStr(ETGPurchPackingSlip, ETGTradingReportFr));
        reportRunController.parmShowDialog(false);
        dataContract = reportRunController.parmReportContract().parmRdpContract() as PurchPackingSlipContract;
        dataContract.parmRecordId(VendPackingSlipJour.RecId);
        dataContract.parmTableId(VendPackingSlipJour.TableId);
              
        srsReportRun = reportRunController.parmReportRun() as SrsReportRunImpl;
        reportRunController.parmShowDialog(false);
        reportRunController.parmReportRun(srsReportRun);
        reportRunController.parmReportContract().parmPrintSettings().printMediumType(SRSPrintMediumType::Screen);
        reportRunController.startOperation();
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>