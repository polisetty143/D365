<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ETGSalesPointStorage</Name>
	<SourceCode>
		<Declaration><![CDATA[
class ETGSalesPointStorage
{
 /*   /// <summary>
    /// Upload file to SharePoint storage
    /// </summary>
    /// <param name = "_hostName">Host name</param>
    /// <param name = "_siteName">Site name</param>
    /// <param name = "_folderPath">Folder path</param>
    /// <param name = "_memoryStream">Memory stream</param>
    /// <param name = "_fileName">File name</param>
    /// <param name = "_override">Override</param>
    public static void uploadFileToSharePointStorage(str _hostName, str _siteName, str _folderPath, System.IO.MemoryStream _memoryStream, str _fileName, boolean _override = false)
    {
        try
        {
            // ADD PARAMETER VALIDATION
            if (!_hostName)
            {
                throw error("Host name cannot be empty");
            }

            if (!_siteName)
            {
                throw error("Site name cannot be empty");
            }

            if (!_folderPath)
            {
                throw error("Folder path cannot be empty");
            }

            if (!_fileName)
            {
                throw error("File name cannot be empty");
            }

            if (!_memoryStream)
            {
                throw error("Memory stream cannot be null");
            }

            str extId = xUserInfo::getExternalId();
            Microsoft.Dynamics.AX.Framework.FileManagement.SharePointDocumentStorageProvider provider;
            Microsoft.Dynamics.AX.Framework.FileManagement.DocumentLocation documentLocation = new Microsoft.Dynamics.AX.Framework.FileManagement.DocumentLocation();

            provider = new Microsoft.Dynamics.AX.Framework.FileManagement.SharePointDocumentStorageProvider(_hostName, _siteName, _folderPath + '/', extId);

            if (_override)
            {
                documentLocation = provider.SaveFileWithOverwrite(newGuid(), _fileName, System.Web.MimeMapping::GetMimeMapping(_fileName), _memoryStream);
            }
            else
            {
                documentLocation = provider.SaveFile(newGuid(), _fileName, System.Web.MimeMapping::GetMimeMapping(_fileName), _memoryStream);
            }
        }
        catch (Exception::Error)
        {
            throw error(strFmt("SharePoint upload failed: %1", infolog.text()));
        }
    }
*/
}
]]></Declaration>
		<Methods />
	</SourceCode>
</AxClass>