<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ETG_SalesOrderLineNotesUploader</Name>
	<SourceCode>
		<Declaration><![CDATA[
using System.IO;
using OfficeOpenXml;
using OfficeOpenXml.ExcelPackage;
using OfficeOpenXml.ExcelRange;

class ETG_SalesOrderLineNotesUploader
{
    

}
]]></Declaration>
		<Methods>
			<Method>
				<Name>main</Name>
				<Source><![CDATA[
    /// <summary>
    /// Runs the class with the specified arguments.
    /// </summary>
    /// <param name = "_args">The specified arguments.</param>
    public static void main(Args _args)
    {
        System.IO.Stream		    stream;
        ExcelSpreadsheetName	    sheeet;
        FileUploadBuild			    fileUpload;
        DialogGroup				    dlgUploadGroup;
        FileUploadBuild			    fileUploadBuild;
        FormBuildControl		    formBuildControl;

        int                         counter;
        InventTable				    inventTable;       
        SalesTable			        salesTable, salesTableUpd;
        SalesLine		            salesLine;
        ItemId					    itemID;
        SalesId                     salesId;
        InventTransId               inventTransId;
        ETG_SalesLineNotes          salesLineNotes;
        ETGNote                     etgNote;
        NoYesId                     noyes;
        LineNum                     lineNum;
        
              
        Dialog dialog = new Dialog("Import Sales Order Line Notes");

        dlgUploadGroup		= dialog.addGroup("Select Excel File to Upload.");
        formBuildControl	= dialog.formBuildDesign().control(dlgUploadGroup.name());
        fileUploadBuild		= formBuildControl.addControlEx(classstr(FileUpload), 'Upload');

        fileUploadBuild.style(FileUploadStyle::MinimalWithFilename);
        fileUploadBuild.fileTypesAccepted('.xlsx');

    
        if (dialog.run() && dialog.closedOk())
        {
            FileUpload fileUploadControl					  = dialog.formRun().control(dialog.formRun().controlId('Upload'));
            FileUploadTemporaryStorageResult fileUploadResult = fileUploadControl.getFileUploadResult();

            if (fileUploadResult != null && fileUploadResult.getUploadStatus())
            {
                stream = fileUploadResult.openResult();
                using (ExcelPackage Package = new ExcelPackage(stream))
                {
                    int rowCount, i;

                    Package.Load(stream);
                    ExcelWorksheet worksheet		= package.get_Workbook().get_Worksheets().get_Item(1);
                    OfficeOpenXml.ExcelRange range	= worksheet.Cells;
                    rowCount = worksheet.Dimension.End.Row - worksheet.Dimension.Start.Row + 1;

                    for (i = 2; i<= rowCount; i++)
                    {
                        
                        salesId         = range.get_Item(i, 1).Value;
                        lineNum         = str2Num(range.get_Item(i, 2).Value);
                        select firstonly salesLine
                            where salesLine.SalesId == salesId
                            && salesLine.LineNum == lineNum;
                        itemID		    = range.get_Item(i, 3).Value;
                        inventTable	    = InventTable::find(itemID,false);
                        salesTable      = SalesTable::find(salesId);
                       
                        select firstonly salesTableUpd
                            where salesTableUpd.SalesId == salesLine.SalesId;
                        
                        if(!salesTable)
                        {
                            error(strFmt('Sales Order %1 does not Exist',salesId));
                        }

                       
                        if(inventTable && salesTable && salesLine)
                        {
                            salesLineNotes.clear();
                          
                            try
                            { 
                                salesLineNotes.InventTransId            = salesLine.InventTransId;
                                salesLineNotes.ItemId                   = range.get_Item(i, 3).Value;
                                salesLineNotes.EnumNotes                = str2Enum(etgNote, range.get_Item(i, 4).Value);
                                salesLineNotes.Description              = range.get_Item(i, 5).Value;
                                salesLineNotes.PrintNotesInContract     = str2Enum(noyes, range.get_Item(i, 6).Value);
                                  
                                ttsbegin;
                                if(salesLineNotes.validateWrite())
                                {
                                    salesLineNotes.insert();
                                }
                                ttscommit;
                                counter++;

                            }
                            catch(Exception::Error)
                            {
                                ttsabort;
                            }
                        }
                        else
                        {
                            info(strFmt('Item ID %1 not exist in released product. Please released the item in current entity and process uploading.' ,itemID));
                        }
                    }
                    info(strFmt('Sales Order Lines Notes Uploaded Sucessfully. Total Lines %1 : ',counter));
                }
            }
            else
            {
                error("Error During Upload. Please Contact System Admin. Item ID %1",itemID);
            }

        }
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>