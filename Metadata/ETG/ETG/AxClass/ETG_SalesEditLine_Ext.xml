<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ETG_SalesEditLine_Ext</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
/// <c>ETG_SalesEditLine_Ext</c>
/// </summary>
class ETG_SalesEditLine_Ext
{

    

}
]]></Declaration>
		<Methods>
			<Method>
				<Name>SalesParmTable_OnValidatedWrite</Name>
				<Source><![CDATA[
    /// <summary>
    ///
    /// </summary>
    /// <param name="sender"></param>
    /// <param name="e"></param>
    [FormDataSourceEventHandler(formDataSourceStr(SalesEditLines, SalesParmTable), FormDataSourceEventType::ValidatedWrite)]
    public static void SalesParmTable_OnValidatedWrite(FormDataSource sender, FormDataSourceEventArgs e)
    {
        /*SalesParmTable salesparmtable = sender.cursor();
        SalesParmUpdate   salesparmupdate = SalesParmUpdate::find(salesparmtable.ParmId);
       // smmParametersTable smm = smmParametersTable::find();
        VendTable                     vendtable;
        InterCompanyTradingPartner    tradingpartner;
        INTERCOMPANYTRADINGRELATION   intercompanyreal;
        ETG_Parameters                parameters;

        str         currentcompanycountry = LogisticsAddressCountryRegion::findByISOCode(SysCountryRegionCode::countryInfo(curext())).CountryRegionId;
        var args = e as FormDataSourceCancelEventArgs;

        vendtable = VendTable::find( salesparmtable.salesTable().interCompanyPurchTable().OrderAccount);
        tradingpartner = InterCompanyTradingPartner::findOrCreateVendor(vendtable.AccountNum,salesparmtable.salesTable().interCompanyPurchTable().DataAreaId);
        select firstonly intercompanyreal where intercompanyreal.InterCompanyTradingVendor == tradingpartner.RecId;
        select firstonly parameters;

        if(parameters.ETG_BLValidations == NoYes::Yes && parameters.ETG_BLValidationAccountType == ETG_BLValidationAccountType::InterCompany && salesparmupdate.DocumentStatus == DocumentStatus::Invoice )
        {
            if(currentcompanycountry != salesparmtable.salesTable().deliveryAddress().CountryRegionId)
            {
                
                if(intercompanyreal.Active == NoYes::Yes && !salesparmtable.ETG_BLDate && !salesparmtable.ETG_BLnumber)
                {
                    boolean doCancel = checkFailed("@ETG:BLnoBLDateValidateMsg");
                    args.cancel(doCancel);
                }
            }

        }
        else if(parameters.ETG_BLValidations == NoYes::Yes && parameters.ETG_BLValidationAccountType == ETG_BLValidationAccountType::ThirdParty && salesparmupdate.DocumentStatus == DocumentStatus::Invoice )
        {
            if(currentcompanycountry != salesparmtable.salesTable().deliveryAddress().CountryRegionId)
            {
                if(intercompanyreal.Active == NoYes::No && !salesparmtable.ETG_BLDate && !salesparmtable.ETG_BLnumber)
                {
                    boolean doCancel = checkFailed("@ETG:BLnoBLDateValidateMsg");
                    args.cancel(doCancel);
                }
            }

        }

        else if(parameters.ETG_BLValidations == NoYes::Yes && parameters.ETG_BLValidationAccountType == ETG_BLValidationAccountType::Both && salesparmupdate.DocumentStatus == DocumentStatus::Invoice )
        {
            if(currentcompanycountry != salesparmtable.salesTable().deliveryAddress().CountryRegionId)
            {
                if(!salesparmtable.ETG_BLDate && !salesparmtable.ETG_BLnumber)
                {
                    boolean doCancel = checkFailed("@ETG:BLnoBLDateValidateMsg");
                    args.cancel(doCancel);
                }
            }

        } */

       
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>